{
  "name": "dnd-cloud-compagnon",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "core",
    "feats-api",
    "spells-api",
    "items-api"
  ],
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "create-table": "tsx scripts/create-table.ts",
    "seed:feats": "tsx scripts/seed-feats.ts",
    "seed:items": "tsx scripts/seed-items.ts",
    "seed:spells": "tsx scripts/seed-spells.ts",
    "seed:all": "npm run seed:feats && npm run seed:items && npm run seed:spells",
    "seed:local": "npm run create-table && npm run seed:all",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
